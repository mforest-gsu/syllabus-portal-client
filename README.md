# mforest-gsu/syllabus-portal-client

Web frontend for Georgia State University's Syllabus Portal.

## Deploying to production

Double-check your `.env` files first, then run:

```shell
$ npm run build -- --base=/SyllabusPortal/ --minify
```
